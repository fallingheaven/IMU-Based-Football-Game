public enum SceneType // 场景的类型
{
    GameScene, Menu
}

public enum BallType
{
    Football,
    Goldball,
    Bomb
};

public enum Direction
{
    Null,
    East,
    West,
    South,
    North
};

public enum Difficulty
{
    Easy,
    Normal,
    Hard,
    Crazy
}
